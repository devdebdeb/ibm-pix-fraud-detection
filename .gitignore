# Credentials — NEVER commit
config/credentials.json
*.env
.env

# Environment-specific generated outputs (deployment IDs, metrics, encoders)
config/deployment_meta.json
config/results.json
config/*.pkl

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# AutoAI SDK scratch artifacts (regenerated on each run)
request.json
notebooks/request.json
pipeline-model.json
notebooks/pipeline-model.json
*.pickle.tar.gz

# Data (too large for git)
data/
*.csv
*.parquet

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
